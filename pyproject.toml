[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = [
    "--cov=collector",
    "--cov=filter",
    "--cov=notifier",
    "--cov=utils",
    "--cov=web",
    "--cov-report=term-missing",
    "--cov-fail-under=70",
]
